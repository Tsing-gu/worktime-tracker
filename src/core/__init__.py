# -*- coding: utf-8 -*-
"""
core - 纯业务逻辑层
====================

本层包含工时计算器的核心业务逻辑，不直接依赖数据库或 UI 层。

- tracker:    HID 空闲时间轮询 + 上下班判定（纯逻辑，返回事件结果）
- calculator: 工时计算（纯函数，接收数据参数不查 DB）
- holiday:    节假日 API 获取 + 本地缓存管理
"""
